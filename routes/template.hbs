<html>

<head>
    <title>Aston University Event Management System</title>
    <style>
        .card {
            position: relative;
            display: -ms-flexbox;
            min-width: 0;
            word-wrap: break-word;
            background-color: #fff;
            background-clip: border-box;
            border: 1px solid rgba(0, 0, 0, 0.125);
            border-radius: 0.25rem; 
        }
        .card-header{
            background: #eb6864;
            padding: 20px;
            color: #fff;
            font-size: 20px;
            font-weight: 600;
        }
        .card-title{
            font-size: 18px;
            padding: 0 20px;
        }

        .card-text{
            padding: 0 20px;
        }
    </style>
</head>

<body>
    <div class="card border-primary">
        <div class="card-header">You have received mail from {{sender}}</div>
        <div class="card-body">
            <h4 class="card-title">{{subject}}</h4>
            <p class="card-text">{{message}}</p>
        </div>
    </div>
</body>

</html>